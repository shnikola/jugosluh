= form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: 'pure-form pure-form-aligned login-form'}) do |f|

  .pure-control-group
    = f.label :email
    = f.email_field :email, autofocus: true 

  .pure-control-group
    = f.label :password
    = f.password_field :password, autocomplete: "off"

  .pure-controls
    - if devise_mapping.rememberable?
      %label.pure-checkbox
        = f.check_box :remember_me
        Remember me

    = f.submit "Log in", class: "pure-button pure-button-primary"
